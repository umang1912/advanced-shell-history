TODO
  [ ] audit existing TODO comments in the code
  [ ] Create a history function in the common shell lib, redirect queries to a custom binary: ash_query.
  [ ] create a Google Sites page
  [ ] add better comments to the header and source files
  [ ] set up the initial repository on code.google.com
  [ ] Makefile - build non-debug archetecture-specific binaries - better optimization
  [ ] write up better documentation - how to install, how to use, db schema, etc.
  [ ] man pages for ash_log and ash_query
  [ ] add a command-time-only flag to ash::log and the other internal-mechanism functions to prevent curious users from invoking them.  Or at least they should see an error.  ASH_KEY="OK" ash::log 
  [ ] 

