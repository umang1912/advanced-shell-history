TODO
  [ ] add an option to remove loopback IPs from the session IP address
  [ ] make the date more useable by adding a date table and linking to the date table
  [ ] change all the .cpp files to only include the headers they need.
  [ ] 

config.cpp
  [ ] support ~/.history.conf and /etc/history.conf ##  config = new Config(home_file, etc_file)
  [ ] support operator [] ##  if (config["foo"] == "") { ...
  [ ] make the config object globally available?
  [ ] 


database.cpp
  [ ] complete the insert retry logic - maybe add a random nano sleep or something
  [ ] 
